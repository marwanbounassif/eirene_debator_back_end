# Example environment variables for local development
# Copy this file to .env and set real values before running

# Example environment variables for local development.
# Copy this file to `.env` and fill the secrets/paths. Do NOT commit `.env`.

# Provider API key (Hugging Face or other inference provider)
HF_API_KEY=

# Model identifier used by the inference client
# Example: meta-llama/Meta-Llama-3-8B-Instruct
MODEL_ID=

# Path to the model config mapping (usually ./configs/model_config.json)
MODEL_CONFIG_PATH=./configs/model_config.json

# Path to the debate prompts/config (usually ./configs/debate_config.json)
DEBATE_CONFIG_PATH=./configs/debate_config.json

# Directory where generated character JSON files are saved
# Make sure this directory exists and is writable by the app
CHARACTER_DUMP_PATH=./characters

# Optional: server port to run uvicorn on (if you use it)
PORT=8000